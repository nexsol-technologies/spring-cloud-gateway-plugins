<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<groupId>ch.nexsol-tech.gateway</groupId>
		<artifactId>spring-cloud-gateway-samples</artifactId>
		<version>${revision}</version>
		<relativePath>../pom.xml </relativePath>
	</parent>
	<artifactId>sample-gateways</artifactId>
	<name>sample-gateways</name>
	<description>Gateway samples, one per plugin plus a few combinations</description>
	<packaging>pom</packaging>

	<modules>
		<!-- One plugin each -->
		<module>gateway-filters</module>
		<module>gateway-openapi-validation</module>
		<module>gateway-oauth2</module>
		<module>gateway-hub-openapi</module>
		<module>gateway-ui</module>
		<module>gateway-ui-secured</module>
		<module>gateway-audit</module>
		<module>gateway-metrics</module>
		<module>gateway-routes-files</module>
		<!-- Combinations -->
		<module>gateway-routes-all</module>
		<module>gateway-secured</module>
		<module>gateway-full</module>
	</modules>

	<!--
		What every gateway sample needs: the gateway itself, and the actuator the plugins
		read the meter registry and the refresh endpoint through. Anything beyond that is
		declared by the sample that exercises it, so each pom reads as the list of plugins
		its scenario is about.
	-->
	<dependencies>
		<dependency>
			<groupId>org.springframework.cloud</groupId>
			<artifactId>spring-cloud-gateway-server-webflux</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-actuator</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-test</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-webtestclient</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>io.projectreactor</groupId>
			<artifactId>reactor-test</artifactId>
			<scope>test</scope>
		</dependency>
	</dependencies>

	<build>
		<plugins>
			<plugin>
				<groupId>org.springframework.boot</groupId>
				<artifactId>spring-boot-maven-plugin</artifactId>
				<version>${spring-boot.version}</version>
			</plugin>
		</plugins>
	</build>

</project>
