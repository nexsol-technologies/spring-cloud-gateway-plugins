server.port: 8080
spring.application.name: service-a
eureka:
  client:
    enabled: false
---
spring:
  config:
    activate:
      on-profile:
      - eureka
eureka:
  client:
    enabled: true
    fetch-registry: true
    register-with-eureka: true